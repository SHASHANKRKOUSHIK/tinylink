datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model Link {
  code        String   @id
  longUrl     String
  clickCount  Int      @default(0)
  lastClicked DateTime?
  createdAt   DateTime @default(now())
}